diff --git a/packages/node/package.json b/packages/node/package.json
index 19a8562..117722c 100644
--- a/packages/node/package.json
+++ b/packages/node/package.json
@@ -24,6 +24,17 @@
     },
     "./package.json": "./package.json"
   },
+  "scripts": {
+    "dev": "tsdx dev",
+    "build": "tsdx build",
+    "test": "vitest run",
+    "test:watch": "vitest",
+    "lint": "tsdx lint",
+    "format": "tsdx format",
+    "format:check": "tsdx format --check",
+    "typecheck": "tsdx typecheck",
+    "prepublishOnly": "pnpm build"
+  },
   "dependencies": {
     "@hono/node-server": "^2.0.0"
   },
diff --git a/packages/node/src/graceful-shutdown.ts b/packages/node/src/graceful-shutdown.ts
index 917b3bb..39f5a67 100644
--- a/packages/node/src/graceful-shutdown.ts
+++ b/packages/node/src/graceful-shutdown.ts
@@ -3,7 +3,7 @@ import { createGracefulShutdown, RuntimeAdapter } from "@buujs/sati/shutdown"
 
 const signals: NodeJS.Signals[] = ["SIGINT", "SIGTERM"]
 
-const nodeRuntime: RuntimeAdapter = {
+export const nodeRuntime: RuntimeAdapter = {
   onShutdown: (handler: (signal: string, error?: Error) => void) => {
     signals.forEach((signal) => {
       process.on(signal, () => {
