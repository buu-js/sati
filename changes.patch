diff --git a/packages/core/package.json b/packages/core/package.json
index 6c9bbde..357dda8 100644
--- a/packages/core/package.json
+++ b/packages/core/package.json
@@ -26,6 +26,16 @@
         "default": "./dist/index.cjs"
       }
     },
+    "./boot": {
+      "import": {
+        "types": "./dist/boot/index.d.ts",
+        "default": "./dist/boot/index.js"
+      },
+      "require": {
+        "types": "./dist/boot/index.d.cts",
+        "default": "./dist/boot/index.cjs"
+      }
+    },
     "./logger": {
       "import": {
         "types": "./dist/logger/index.d.ts",
diff --git a/packages/core/src/scheduler.ts b/packages/core/src/scheduler.ts
index 0610fca..8f790c3 100644
--- a/packages/core/src/scheduler.ts
+++ b/packages/core/src/scheduler.ts
@@ -6,13 +6,12 @@ export type SchedulerConfig = {
   logger: ReturnType<typeof createLogger>
 }
 
+export type ScheduleConfig = { cronTime: string; name: string }
+
 export interface Scheduler {
-  register(
-    { cronTime, name }: { cronTime: string; name: string },
-    fn: () => void | Promise<void>
-  ): void
+  register(config: ScheduleConfig, fn: () => void | Promise<void>): Scheduler
   trigger(): void
   stop(): void
 }
 
-export type SchedulerFactory = ({ logger }: SchedulerConfig) => Scheduler
+export type SchedulerFactory = (config: SchedulerConfig) => Scheduler
diff --git a/packages/croner/package.json b/packages/croner/package.json
index 95f2052..b01827c 100644
--- a/packages/croner/package.json
+++ b/packages/croner/package.json
@@ -33,7 +33,7 @@
     "format": "tsdx format",
     "format:check": "tsdx format --check",
     "typecheck": "tsdx typecheck",
-    "prepublishOnly": "bun run build"
+    "prepublishOnly": "pnpm run build"
   },
   "dependencies": {
     "croner": "^10.0.1"
diff --git a/packages/croner/src/index.ts b/packages/croner/src/index.ts
index c6faeb1..1f67b03 100644
--- a/packages/croner/src/index.ts
+++ b/packages/croner/src/index.ts
@@ -1,4 +1,4 @@
-import { SchedulerFactory, Scheduler } from "@buujs/sati/scheduler"
+import { SchedulerFactory, Scheduler, ScheduleConfig } from "@buujs/sati/scheduler"
 import { Cron } from "croner"
 import { assert } from "@buujs/sati-utils"
 
@@ -15,7 +15,7 @@ export const createScheduler: SchedulerFactory = ({ gracefulShutdown, logger })
     })
 
     return {
-      register({ name, cronTime }, fn) {
+      register({ name, cronTime }: ScheduleConfig, fn: () => void | Promise<void>) {
         assert(!!cronTime, "Missing Cron Time configuration")
 
         if (job) {
diff --git a/packages/node/package.json b/packages/node/package.json
index 117722c..ab1d90c 100644
--- a/packages/node/package.json
+++ b/packages/node/package.json
@@ -42,7 +42,8 @@
     "@types/node": "^22.20.1"
   },
   "peerDependencies": {
-    "@buujs/sati": "workspace:*"
+    "@buujs/sati": "workspace:*",
+    "@buujs/sati-croner": "workspace:*"
   },
   "engines": {
     "node": ">=20"
diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
index 25a5efa..f19a288 100644
--- a/pnpm-lock.yaml
+++ b/pnpm-lock.yaml
@@ -88,6 +88,9 @@ importers:
       '@buujs/sati':
         specifier: workspace:*
         version: link:../core
+      '@buujs/sati-croner':
+        specifier: workspace:*
+        version: link:../croner
       '@hono/node-server':
         specifier: ^2.0.0
         version: 2.0.8(hono@4.12.29)
